{
  "name": "shortcut-worker",
  "version": "1.0.0",
  "description": "Shortcut Background Job Worker",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "clean": "rimraf dist && mkdir dist",
    "watch": "yarn run clean && tsc -w",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/FeelTrainCoop/shortcut.git"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "bull": "^3.7.0"
  },
  "devDependencies": {
    "@types/bull": "^3.5.8",
    "@types/dotenv": "^6.1.0",
    "@types/node": "^11.11.3",
    "dotenv": "^7.0.0",
    "rimraf": "^2.6.3",
    "typescript": "^3.3.3333"
  }
}
